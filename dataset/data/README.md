# 数据文件目录

存放数学建模竞赛所用的结构化数据（CSV / Excel）。

| 子目录 | 用途 |
|--------|------|
| `raw/` | 赛题原始数据，保持原样不修改 |
| `processed/` | 经代码处理后的中间数据 |
| `external/` | 手动下载的外部公开数据集 |

> 临时上传给 agent 分析的文件，请放到 `inbox/data/` 而不是这里。
> 这里存放需要长期保留、可跨 session 复用的数据。
